# Base URL of the backend API. Left blank / relative in development
# since vite.config.js proxies /api/v1 to the backend, avoiding CORS
# entirely on localhost. Set to the deployed backend origin in production.
VITE_API_BASE_URL=/api/v1
